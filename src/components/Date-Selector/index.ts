import { DaySelector } from "@/components/Date-Selector/day-selector";
import { MonthSelector } from "@/components/Date-Selector/month-selector";
import { YearSelector } from "@/components/Date-Selector/year-selector";
import { RootSelector } from "@/components/Date-Selector/date-selector-root";

export const DateSelector = {
  Root: RootSelector,
  Day: DaySelector,
  Month: MonthSelector,
  Year: YearSelector,
};
